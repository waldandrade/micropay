{
  "name": "micropay",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "dev": "tsx watch --env-file=.env server.ts",
    "debug": "tsx watch --env-file=.env --inspect-brk ./server.ts",
    "swagger": "tsx --env-file=.env swagger.js"
  },
  "devDependencies": {
    "@types/node": "^22.11.0",
    "swagger-autogen": "^2.23.7",
    "tsx": "^4.20.3"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "swagger-ui-express": "^5.0.1"
  }
}

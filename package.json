{
  "name": "micropay",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "dev": "tsx watch --env-file=.env server.ts",
    "debug": "tsx watch --env-file=.env --inspect-brk server.ts"
  },
  "devDependencies": {
    "@types/node": "^24.2.0",
    "tsx": "^4.20.3"
  }
}
